import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    carImageContainer: {
        flex: 1,
        marginBottom: 10,
        justifyContent: "center",
        alignItems: "center",
    },
    carImage: {
        width: 330,
        height: 330,
        resizeMode: "contain",

    },
});